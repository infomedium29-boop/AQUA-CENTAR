const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const isOpen=nav.classList.toggle('open');menuButton.classList.toggle('open',isOpen);menuButton.setAttribute('aria-expanded',String(isOpen))});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}))}const revealItems=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.14});revealItems.forEach(item=>observer.observe(item));const quoteForm=document.getElementById('quoteForm');if(quoteForm){quoteForm.addEventListener('submit',event=>{event.preventDefault();const form=new FormData(quoteForm);const subject=encodeURIComponent('Upit s web stranice - Aqua Centar Zagorje');const body=encodeURIComponent(`Ime i prezime: ${form.get('name')||''}\nE-mail: ${form.get('email')||''}\nTelefon: ${form.get('phone')||''}\nUsluga: ${form.get('service')||''}\n\nPoruka:\n${form.get('message')||''}`);window.location.href=`mailto:info@aquacentar-zagorje.hr?subject=${subject}&body=${body}`})}